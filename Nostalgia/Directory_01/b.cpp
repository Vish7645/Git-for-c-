#include <iostream>
using namespace std;

int  main()
{
   bool b1;//true or false 1 bit
   char ch='a';//we should use ''to give right output
   
   int a,b,c;//declaration of variable takes 4 bytes and store in integer datatype
   //32 bits    -2^31 to 2^31 -1
   //unsigned int  0 to 2*32 -1
   
   // Long int = 4 bytes
   // Long Long int = 8 bytes
   //float f = 0.0;// 9th place of decimal
   double d = 56.64378; // 8 bytes upto 15 place of decimal
   long double ld = 5.98;// For more precisation
   //string str = "0";
   string str ="Vishal";// so if we need muntiple variable use at a time then we this
   // 'By defult fraction like 0.0 are act double unless mention outher data type', 0 = integer , '0'= char , "0" store as string
   
   //a=1;// Initailisatio0n
   //b=a;

   //cin>>a;
   //cin>>b;
   //cin>>c;

   // cout<<a+b+c;


    
}

